# Dependências
node_modules/
.pnp
.pnp.js

# Build e dist
dist/
dist-ssr/
build/
*.local

# Locks (não precisam de contexto)
bun.lock
package-lock.json
yarn.lock
pnpm-lock.yaml

# Variáveis de ambiente (segurança)
.env
.env.local
.env.*.local
.env.production

# Git
.git/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Cache e temporários
.cache/
.vite/
*.tsbuildinfo

# Supabase gerado automaticamente (não editar manualmente)
src/integrations/supabase/types.ts

# Playwright (gerado por testes)
playwright-report/
test-results/
